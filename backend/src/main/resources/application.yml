server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/baby_diary?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: baby-diary-jwt-secret-key-2024-change-in-production
  expiration: 604800000

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: baby-diary

deepseek:
  api-key: sk-2bcf664b8207499bb8b340a7afe2fc46
  base-url: https://api.deepseek.com
  model: deepseek-chat
